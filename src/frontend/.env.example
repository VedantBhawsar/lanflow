# Agent Node Default Configuration for Builder-Only Mode
# Copy this file to .env and fill in with your actual values

# OpenAI API Key (or your LLM provider's API key)
VITE_DEFAULT_AGENT_API_KEY=sk-your-actual-api-key-here

# LLM Provider (e.g., OpenAI, Anthropic, etc.)
VITE_DEFAULT_AGENT_LLM_PROVIDER=OpenAI

# Model Name (e.g., gpt-4o, gpt-3.5-turbo, claude-3-opus, etc.)
VITE_DEFAULT_AGENT_MODEL_NAME=gpt-4o-mini
